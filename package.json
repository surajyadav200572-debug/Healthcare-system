{
  "name": "healthcare-system-root",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "npm --prefix backend start",
    "build": "npm --prefix backend install"
  }
}
